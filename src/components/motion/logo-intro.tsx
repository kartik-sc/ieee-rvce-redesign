"use client";

// The hero section now owns the full cinematic opening sequence.
// This component is kept as a placeholder to avoid breaking page.tsx imports.
export function LogoIntro() {
  return null;
}
